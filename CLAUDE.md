# Knowledge Graph Research Engine — Project Context

The project context lives in a single canonical file, [AGENTS.md](AGENTS.md), so
that Claude Code and Codex read the same instructions and never drift apart.
The import below pulls it in. Do not duplicate content here — edit AGENTS.md.

@AGENTS.md
