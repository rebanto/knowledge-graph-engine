[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    neo4j: integration tests that require a running Neo4j (auto-skipped if unavailable)
